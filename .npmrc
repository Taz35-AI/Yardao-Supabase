# @capacitor/camera@8 declares a peer of @capacitor/core@>=8, but this project
# is pinned to the Capacitor 7 line. That's the same (intentional) setup the app
# has always shipped with, so we relax peer-dep resolution everywhere — local,
# CI, and Vercel — instead of changing the Capacitor stack.
legacy-peer-deps=true
